# Wi-Fi credentials — copy this file to .env and fill in your values
WIFI_SSID=your_network_name
WIFI_PASS=your_password
# Shared-secret token for mutating API operations (PATCH, DELETE, OMI writes)
API_TOKEN=your_secret_token
