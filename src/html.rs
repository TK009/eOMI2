// HTML parser — extract <script> tags from HTML payloads.
//
// Future: lightweight, allocation-conscious parser.
// Candidates: `tl` (fast zero-copy) or a custom minimal parser
// that only extracts <script> content.
